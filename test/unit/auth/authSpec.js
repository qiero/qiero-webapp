'use strict';describe('Auth module specs', function() {  module(function($provide) {  $provide.service('hoodieAccount', function() {    this.signIn = jasmine.createSpy('signIn').andCallFake(function(username, password) {      //a fake implementation    });  });});  beforeEach(module('auth'));  describe('LoginController', function() {    it('should reset the form after submit', inject(function($controller) {        var scope = $rootScope.$new();                var ctrl = $controller('LoginController', {$scope: scope});                scope.username = 'testUsername';        scope.password = 'testPassword';              expect(scope.username).toBe('testUsername');        expect(scope.password).toBe('testPassword');                ctrl.login();                expect(scope.username).toEqualData('');        expect(scope.password).toEqualData('');          }));  });});