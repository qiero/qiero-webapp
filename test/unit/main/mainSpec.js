'use strict';describe('Main module specs', function() {  beforeEach(module('main'));  describe('MainController', function() {    it('should have Qiero as helloText', inject(function($controller) {        var scope = $rootScope.$new();                var ctrl = $controller('MainController', {$scope: scope});        expect(scope.helloText).toBe('Qiero');          }));  });});